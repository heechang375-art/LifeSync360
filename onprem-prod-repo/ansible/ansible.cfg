[defaults]
inventory          = inventory/hosts.yml
remote_user        = ansible
host_key_checking  = False
roles_path         = roles
stdout_callback    = yaml

[privilege_escalation]
become      = True
become_method = sudo
become_user = root
